# Git Commit Message

```
feat: 호스트의 참여자 강퇴 기능 및 자동 게임 종료 로직 개선

## 주요 변경사항

### 1. 참여자 강퇴 기능 (Kick) 추가
- 호스트가 접속자 목록에서 다른 사용자를 더블 클릭하여 강퇴 가능
- 게임 진행 중일 경우, 아직 주사위를 굴리지 않은 사람만 강퇴 가능하도록 보호 로직 적용
- 강퇴된 사용자에게는 알림창을 표시하고 로비로 자동 이동

### 2. 자동 게임 종료 조건 체크 강화
- 참여자가 강퇴되거나 방을 나갈 때마다 게임 종료 조건(모두 굴림 여부)을 즉시 체크
- 남은 참여자가 모두 굴린 상태가 되면 자동으로 게임이 종료되고 결과 정산

### 3. 코드 구조 개선 및 리팩토링
- 중복되던 게임 종료 체크 로직을 `checkAndEndGame` 공통 함수로 통합
- `leaveRoom` 이벤트 처리 시 게임 참여자 목록(`gamePlayers`) 및 준비 목록(`readyUsers`) 자동 정리 로직 추가

## 기술적 변경사항

### 서버 측 (server.js)
- `kickPlayer` 소켓 이벤트 핸들러 구현
- `checkAndEndGame(gameState, room)` 공통 함수 추가
- `leaveRoom` 함수 내 참여자 목록 정리 및 종료 체크 로직 통합

### 클라이언트 측 (dice-game-multiplayer.html)
- `updateUsers` 함수 내 사용자 태그에 `dblclick` 이벤트 리스너 추가 (호스트 전용)
- `kicked` 소켓 이벤트 수신 및 처리 로직 추가

## UX 개선
- 비매너 사용자나 잠수 사용자를 호스트가 직접 관리할 수 있어 쾌적한 게임 환경 제공
- 중도 퇴장자가 발생해도 게임이 멈추지 않고 즉시 결과가 나오도록 개선
```
