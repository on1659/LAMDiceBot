# 주사위 게임 테스트 봇 사용 가이드

## 🧪 테스트 방법

### 1단계: 서버 실행

**터미널 1**에서 서버를 실행합니다:

```bash
node server.js
```

### 2단계: 테스트 봇 실행

**터미널 2**에서 테스트 봇을 실행합니다:

#### Windows
```bash
cd AutoTest/dice
test-bot.bat
```

#### Linux/Mac
```bash
cd AutoTest/dice
chmod +x test-bot.sh
./test-bot.sh
```

### 3단계: 웹 브라우저에서 확인

브라우저에서 `http://localhost:3000`에 접속하여 게임이 정상적으로 진행되는지 확인합니다.

## 📊 콘솔 로그 확인

테스트 봇의 콘솔에서 다음 로그를 확인할 수 있습니다:

- `✅ 연결 성공` - 봇이 서버에 연결됨
- `🎉 방 생성 성공` - 호스트 봇이 방을 생성함
- `🚪 방 입장 성공` - 봇이 방에 입장함
- `🎮 게임 시작!` - 게임이 시작됨
- `🎲 주사위 굴리는 중...` - 봇이 주사위를 굴림
- `💬 채팅 전송` - 봇이 채팅을 보냄
- `😊 이모티콘 반응 추가` - 봇이 이모티콘 반응을 추가함
- `🏁 게임 종료` - 게임이 종료됨
- `🔄 게임 자동 재시작...` - 게임이 자동으로 재시작됨

## 🔍 문제 해결

### 서버 연결 실패
- 서버가 실행 중인지 확인하세요
- 서버 URL이 올바른지 확인하세요 (`BOT_CONFIG.serverUrl`)

### 봇이 방에 입장하지 않음
- 방 이름이 올바른지 확인하세요 (`BOT_CONFIG.roomName`)
- 서버 로그를 확인하여 오류가 있는지 확인하세요

### 게임이 시작되지 않음
- 호스트 봇이 정상적으로 연결되었는지 확인하세요
- 준비 상태가 올바르게 설정되었는지 확인하세요

## ⚙️ 고급 설정

### 봇 개수 조절
```javascript
botCount: 3, // 원하는 봇 개수로 변경
```

### 자동 기능 비활성화
```javascript
autoRoll: false, // 자동 주사위 굴리기 비활성화
autoChat: false, // 자동 채팅 비활성화
autoReaction: false, // 자동 이모티콘 반응 비활성화
autoRestart: false, // 자동 재시작 비활성화
```

### 지연 시간 조절
```javascript
rollDelay: 2000, // 주사위 굴리기 지연 시간 (ms)
chatDelay: 3000, // 채팅 전송 지연 시간 (ms)
reactionDelay: 4000, // 이모티콘 반응 지연 시간 (ms)
restartDelay: 5000, // 게임 재시작 지연 시간 (ms)
```
